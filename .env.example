# 🔲 ImobiPRO - Variáveis de Ambiente

# ============================================================================
# SUPABASE CONFIGURATION
# ============================================================================
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# ============================================================================
# N8N INTEGRATION
# ============================================================================
# URL do webhook n8n para criação de leads
NEXT_PUBLIC_N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/lead-create

# Define se n8n é o método primário (true) ou fallback (false)
NEXT_PUBLIC_USE_N8N_PRIMARY=false

# Token de autenticação para webhooks n8n (opcional)
NEXT_PUBLIC_N8N_WEBHOOK_TOKEN=your-webhook-token

# ============================================================================
# LEAD SYSTEM CONFIGURATION
# ============================================================================
# Timeout para webhooks em milissegundos (default: 10000)
NEXT_PUBLIC_WEBHOOK_TIMEOUT=10000

# Habilita logs detalhados para debugging
NEXT_PUBLIC_DEBUG_LEADS=false

# ============================================================================
# OPTIONAL FEATURES
# ============================================================================
# URL base da API interna (se diferente do domínio)
NEXT_PUBLIC_API_BASE_URL=

# Habilita modo de desenvolvimento
NEXT_PUBLIC_DEV_MODE=false