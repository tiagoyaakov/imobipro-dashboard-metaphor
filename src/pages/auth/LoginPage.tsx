import React from 'react';
import { LoginBypass } from '@/components/auth/LoginBypass';

// -----------------------------------------------------------
// Página de Login (Temporariamente usando Bypass)
// -----------------------------------------------------------

export const LoginPage: React.FC = () => {
  return <LoginBypass />;
};

export default LoginPage; 